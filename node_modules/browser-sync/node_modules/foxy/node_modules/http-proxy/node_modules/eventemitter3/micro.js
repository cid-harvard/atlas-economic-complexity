var EE = require('./');
var ITER = 1e7;

var ee = new EE();
ee.on('test', function noop() { });

var t = process.hrtime();
for (var i = 0; i < ITER; i++)
  ee.emit('test');
t = process.hrtime(t);

console.log(((t[0] * 1e9 + t[1]) / ITER).toFixed(1) + ' ns/op');
