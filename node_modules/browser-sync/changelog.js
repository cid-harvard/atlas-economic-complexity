require("conventional-changelog")({
    from: "",
    repository: "https://github.com/shakyShane/browser-sync",
    version: require("./package.json").version
}, function (err, log) {
    require("fs").writeFileSync("./CHANGELOG.md", log);
});
